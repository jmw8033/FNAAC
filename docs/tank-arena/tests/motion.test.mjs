import test from 'node:test';import assert from 'node:assert/strict';import {smoothPose,snapshotPair} from '../account/motion.mjs';
test('full-speed movement advances every display frame and teleports snap',()=>{let view=null,last=0;const increments=[];for(let frame=0;frame<120;frame++){const target={x:Math.floor(frame/2)*6,y:0,body:0,angle:0};view=smoothPose(view,target,1/60);if(frame>30)increments.push(view.x-last);last=view.x;}assert(increments.every(n=>n>1&&n<5));assert.equal(smoothPose(view,{x:1000,y:1000,body:0,angle:0},1/60).x,1000);});
test('remote interpolation uses server ticks between snapshots',()=>{const frames=[{at:1000,state:{tick:30}},{at:1075,state:{tick:32}},{at:1130,state:{tick:34}}];const p=snapshotPair(frames,1140);assert.equal(p.a.state.tick,30);assert.equal(p.b.state.tick,32);assert(p.f>0&&p.f<1);});
